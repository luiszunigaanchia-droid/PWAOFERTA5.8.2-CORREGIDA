## 2026-08-07 - Added sr-only label to centerSearchInput
**Learning:** Verified the existence of the `sr-only` class in `styles.css` and successfully applied it to a label to improve accessibility for screen readers without visually altering the UI.
**Action:** Always prefer using an explicitly linked `label` element with an `sr-only` class over `aria-label` for form inputs to maintain semantic HTML and maximum screen reader compatibility, ensuring the visual layout is not disrupted.
